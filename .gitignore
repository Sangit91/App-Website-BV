# Dependencies
node_modules/

# Build outputs
dist/
dist-server/

# Environment files
.env
.env.local
.env.*.local
.env.production
.env.*.example
!.env.example

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Backup folders
*_backup_*/
*_backup/

# Zip files
*.zip
*.rar

# Vite cache
.vite/

# TypeScript cache
*.tsbuildinfo
/src/generated/prisma
/server/generated/prisma

# SSL certs/keys (dev self-signed — không commit private key)
certs/
nginx/ssl/

# OpenBrain local DB (agent persistent memory — state, không track)
openbrain/

# Debug output
inspect-text.txt

# Test output
test-results/
playwright-report/
playwright/.cache/

# Legacy data migration (nguồn excel + upload + file PDF sinh ra — không commit dữ liệu)
Data Migration/
public/tenders/

# Agent/IDE config tool dirs (config local — không track)
.claude/
.windsurf/
.agents/
opencode.json
skills-lock.json

# Loop system state (agent loop runtime files — state local, không track)
.grok/
LOOP.md
STATE.md
loop-budget.md
loop-constraints.md
loop-run-log.md
